для работы нужны библиотеки питона: pyxmpp,MySQLdb
и база my sql с именем emi и пользователем emi

плагины представляют собой файл лежащий в папочке plugins с расширением py и классом с таким же именем внутри для него
обязательны следующие функции 


def init(bot):
	'''
	где bot это TJabberBot 
	здесь можно провести инициализацию плагина или запустить потоки плагина
	должна вернуть уровень доступа(целое число от 0 до +1000 чем больше уровень тем доступнее плагин) 
	для плагина 
	'''
	return 10

def run(bot,user,message):
	'''
	где bot это TJabberBot 
	user это пользователь приславший сообщение user.getNode() даст имя user.getDomain() даст сервер
	message сообщение которое прислали
	bot.sendMessage(user,"тпривет ))") а вот так можно отослать сообщение пользователю user
	'''
	

def help():
	'''
	должна возвращать сравку по плагину 
	'''
	return "текстовая строка "
